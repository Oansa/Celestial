type User = 
 record {
   email: text;
   first_name: text;
   internetId: text;
   second_name: text;
 };
service : {
  deleteUser: (principal) -> ();
  getUser: (principal) -> (opt User) query;
  registerUser: (principal, User) -> ();
}
